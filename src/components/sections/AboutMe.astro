---
import horseback from '@/assets/horseback.webp';
import IllinoisLogo from '@/assets/IllinoisLogo.png';
import CaptionedImage from './CaptionedImage.astro';
import { Image } from 'astro:assets';
---

<div id="about-me">
  <div id="horseback" class="section">
    <CaptionedImage
      src={horseback}
      alt="A very cool guy sitting on a horse statue"
      caption="Me (valiantly) always looking for opportunity"
    />
  </div>
  <p id="about-me-text" class="section">
    I'm in my 4th year studying computer science at the University of Illinois
    at Urbana-Champaign. My <a href="#My-Toolbox">skills</a> are most developed in
    the backend, but I have <a href="#Experience">experience</a>
    working with the entire stack, from frontend to infra.
  </p>
  <p id="projects-paragraph" class="section">
    I love to learn by doing, so I'm always exploring ideas and interesting tech
    by working on side <a href="#Projects">projects</a> across a variety of domains.
  </p>
  <div id="banner" class="section"></div>
  <div id="ILL" class="section">
    <Image src={IllinoisLogo} alt="ILL" />
  </div>
</div>

<style>
  a {
    background-color: #0077b5;
    color: #f5f5f5;
    padding: 0.25rem 0.5rem;
    border-radius: 0.5rem;
  }
  a:hover {
    opacity: 0.9;
  }
  #about-me {
    height: 80vh;
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    grid-template-rows: repeat(12, 1fr);
    gap: 0.5rem;
    border: 1px solid red;
  }
  .section {
    border: 1px solid red;
    overflow: hidden;
  }
  #banner {
    grid-row: 3 / 7;
    grid-column: 1 / 9;
  }
  #about-me-text {
    grid-row: 1 / 3;
    grid-column: 1 / 9;
  }
  #projects-paragraph {
    grid-row: 7 / 9;
    grid-column: 6 / 12;
  }
  #reading-list {
    grid-row: 8 / 13;
    grid-column: 9 / 13;
    overflow: hidden;
  }
  #horseback {
    grid-row: 1 / 7;
    grid-column: 9 / 13;
    animation: bubbleLeft 1.5s ease-in-out 1;
  }
  #ILL {
    grid-row: 7 / 9;
    grid-column: 12 / 13;
  }

  @keyframes bubbleLeft {
    0% {
      transform: scale(0.85);
    }
    40%,
    50% {
      transform: scale(1.05);
      rotate: -5deg;
    }
    80% {
      transform: scale(1.05);
      rotate: -2deg;
    }
    100% {
      transform: scale(1);
    }
  }
</style>
